# 複数物件一括シミュレーション・帳票一括出力機能 要件定義書

## 1. 概要・目的

本機能は、ユーザーが指定のフォーマット（Excelファイル）に複数物件のパラメータ（価格・利回り・融資条件等）をまとめて入力・アップロードすることで、システム側で一括計算（バッチ処理）を行い、各物件の「詳細収支CSV」および「分析レポートPDF」を一元的に一括出力（ZIPアーカイブ化）することを目的とする。

---

## 2. 処理フロー

```
 [ユーザー] ───────────────> [システム (Web)] ───────────────> [ユーザー]
 Excelテンプレートダウンロード  ファイルアップロード & バリデーション    ZIPファイルのダウンロード
 (複数物件データを入力)       (一括計算 & CSV/PDFを生成)           (各物件のCSV/PDFを格納)

```

1. ユーザーが指定の**Excel入力テンプレート**をダウンロードし、複数物件のデータを入力する。
2. Web画面から編集済みExcelファイルをアップロードする。
3. システムがファイルを解析・バリデーション（入力チェック）し、全物件の収支・キャッシュフロー・出口（IRR）シミュレーションを一括計算する。
4. 各物件ごとの「分析レポートPDF」および「年次明細CSV」を自動生成し、1つの**ZIPファイル**にまとめてダウンロード提供する。

---

## 3. 入力仕様（Excelテンプレートフォーマット）

入力Excelファイル（`.xlsx`）の1シート目（シート名: `Property_List`）に、1行1物件形式で以下の列項目を設定する。

| 列 | 項目名（ヘッダー） | 単位 | 必須 | バリデーション・条件 |
| --- | --- | --- | --- | --- |
| A | 物件ID | - | **必須** | 一意の文字列（英数字・ハイフン）。ファイル名に使用 |
| B | 物件名称 | - | **必須** | 文字列（PDF表紙・レポート名に使用） |
| C | 物件価格 | 万円 | **必須** | 正の数値 |
| D | 建物価格割合 | % | 任意 | 0〜100（空欄時はデフォルト 50%） |
| E | 建物法定耐用年数 | 年 | **必須** | 1〜50の整数 |
| F | 想定表面利回り | % | **必須** | 正の数値 |
| G | 返済方式 | - | 任意 | 「元利均等」または「元金均等」（空欄時は元利均等） |
| H | 頭金割合 | % | 任意 | 0〜100（空欄時はデフォルト 0%） |
| I | 借入金利 | % | **必須** | 0以上の数値 |
| J | 借入期間 | 年 | **必須** | 1〜50の整数 |
| K | 想定空室率 | % | 任意 | 0〜100（空欄時はデフォルト 5%） |
| L | 家賃下落率 | % / 年 | 任意 | 0〜10（空欄時はデフォルト 1%） |
| M | 運営管理費率 | % | 任意 | 収入に対する割合（空欄時はデフォルト 15%） |
| N | その他経費率 | % | 任意 | 収入に対する割合（空欄時はデフォルト 5%） |
| O | 保有・売却予定年数 | 年 | 任意 | 1〜35の整数（空欄時は 10年） |
| P | 売却時想定出口利回り | % | 任意 | 正の数値（空欄時は 表面利回り+0.5%） |

---

## 4. 機能要件

### 4.1. テンプレートダウンロード機能

* 適切なヘッダー列、サンプルデータ（1〜2行）、入力規則（ドロップダウン等）が組み込まれた標準Excelテンプレートファイル（`.xlsx`）をWeb画面からダウンロード可能とする。

### 4.2. ファイル解析・バリデーション機能

* **フォーマットチェック**:
* アップロードされたファイルが `.xlsx` / `.xls` 形式であるか確認。
* 必須列（物件ID、物件名、価格、金利等）が存在するか確認。


* **データ不備エラー処理**:
* 行単位で入力チェックを実施。エラーがある場合は計算を中止し、画面上に「◯行目：物件価格が入力されていません」等のエラーメッセージを一覧表示する。



### 4.3. 一括計算処理（バッチエンジン）

* アップロードされたExcel内の全物件について、Phase 1およびPhase 2で定義した計算ロジック（PL・CF・BS・元利/元金均等返済・減価償却・売却IRR等）を実行する。
* 処理制限: 1回のアップロードにつき最大 **50物件** まで処理可能とする（レスポンス確保のため）。

### 4.4. 一括出力・ファイル生成機能

計算完了後、以下の構成でフォルダ分けされた **1つのZIPファイル**（例: `simulation_results_YYYYMMDD.zip`）を自動生成する。

#### ZIPファイル内のフォルダ・ファイル構成

```text
simulation_results_20260904.zip
├── 01_PDF_Reports/                      # レポートPDF格納フォルダ
│   ├── 物件ID001_島根ハイツ_分析レポート.pdf
│   ├── 物件ID002_横浜マンション_分析レポート.pdf
│   └── ...
├── 02_CSV_Details/                      # 年次詳細CSV格納フォルダ
│   ├── 物件ID001_島根ハイツ_年次明細.csv
│   ├── 物件ID002_横浜マンション_年次明細.csv
│   └── ...
└── 全物件一覧サマリー.xlsx (または .csv)  # 各物件の主要指標を一覧化した集計表

```

* **全物件一覧サマリーの内容**:
* 全物件の基本条件、初年度CF、10年累計CF、デッドクロス発生年、売却時手残り、IRRなどを1行ごとにまとめたエグゼクティブサマリーシート。



---

## 5. 画面UI/UX要件

### 一括処理専用画面（またはタブ）

1. **ステップ1: テンプレート取得**
* 「Excelテンプレートをダウンロード」ボタン


2. **ステップ2: ファイル選択・アップロード**
* ドラッグ＆ドロップ対応のファイルアップロードエリア


3. **ステップ3: プレビュー＆実行確認**
* ファイル読み込み後、読み込んだ物件数（例：「12件の物件データが読み込まれました」）と各物件の簡易リストをプレビュー表示。
* 「一括シミュレーション実行」ボタン


4. **ステップ4: 進捗表示＆ダウンロード**
* 計算・PDF生成中のプログレスバー（「5/12 件完了...」）
* 完了後、「全結果をZIPでダウンロード」ボタンを表示。



---

## 6. 非機能要件・技術仕様

### 6.1. 処理方式（フロントエンド処理 vs バックエンド処理）

* **推奨アーキテクチャ**:
* 処理負荷（特に複数PDFの生成）が高くなるため、**バックエンド（Python / Node.js等）での非同期処理またはサーバーレス処理**を推奨。
* **Python構成例**:
* Excel解析: `pandas` / `openpyxl`
* PDF生成: `WeasyPrint` や `ReportLab`（高速で綺麗なPDFを出力可能）
* ZIP化: `zipfile` 標準ライブラリ




* **レスポンス時間**: 10物件のPDF/CSV生成・ZIP化処理を 10秒以内 に完了すること。

### 6.2. 文字コード・ファイル名要件

* CSVファイルの文字コードは **UTF-8（BOM付き）** とする（Excelで開いた際の手戻り・文字化け防止）。
* PDF/CSVのファイル名に含まれる特殊文字（`/ \ : * ? " < > |`）は自動的にハイフンやアンダースコアに置換・サニタイズする。